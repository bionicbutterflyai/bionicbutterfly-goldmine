*TAGS: build, coaching | AUDIENCE: founder + every future Claude (the AREA-8 deep spec — the indicator's meat). This GROWS; each component gets fuller over time + a build-status.*
*CREATED: 2026-06-09, Chat 6 | UPDATED: 2026-06-09, Chat 6 | STATUS: living (several components SPEC-NEEDED — see statuses)*
*SUPERSEDES: — | RELATED: build_blueprint (area 8 points here), bionic_lab_spec.md (the read is taught, not the tool), strat_zone_taxonomy.md, two_strategy_split.md*

# GOLD — 8 · THE QUBED INDICATOR (TradingView) — deep spec

## 8.0 — ONE-LINE + STATUS KEY
The bioniq charting layer: one (or few) consolidated, invite-only TradingView indicator(s) that plot the
Qubed read. This doc holds each component: **what it plots · what it means · why it matters to the strat ·
build-status.** Status key: ✅ DONE · 🛠️ BORROWED-NEEDS-REWRITE · 📝 SPEC-NEEDED (Mark to pour) · ⏳ NOT-BUILT.
**Principle (from bionic_lab_spec.md): we teach what it's TELLING us per the strat — never the internals or
provenance. The indicator is the instrument; the interpretation is the coaching.**

## 8.1 — Consolidate into one (or few) "Qubed" indicators · 🛠️ + ⏳
- WHAT: collapse the borrowed-then-tweaked pile into one clean, branded indicator (or a small set).
- WHY IT MATTERS: stops the leaky bucket (a pile of open indicators = the strat walks out the door).
- STATUS: borrowed pieces exist, not yet consolidated/rewritten. *(detail to grow)*

## 8.2 — Invite-only / protected publish · ⏳
- WHAT: Pine Script invite-only — source hidden, access granted per-student.
- WHY: a wall around the instrument (note: the moat is the coaching loop, not the script).
- STATUS: not built. *(detail to grow)*

## 8.3 — Easy settings + smart defaults · ⏳
- WHAT: minimal settings panel; works out of the box; **auto dark/light** so it looks right on any chart.
- WHY: "default-it-so-it-just-works" — students shouldn't fiddle.
- STATUS: not built. *(detail to grow)*

## 8.4 — QUARTERLY LINES · 📝 SPEC-NEEDED (Mark to pour)
- WHAT: *[Mark: what are the quarterly lines? how are they drawn / calculated?]*
- WHAT IT MEANS / READS: *[what do they tell us?]*
- WHY IT MATTERS TO THE STRAT: *[how does it feed a Qubed decision — Question/Qualify/Quantify?]*
- STATUS: **explained to past Claudes but NEVER captured or built — this jar is the fix.** Pour it here and
  it stops evaporating. Then status → spec-captured / NOT-BUILT.

## 8.5 — Live setup grading on the chart · ⏳
- WHAT: the indicator surfaces the Qubed grade (or its inputs) live on the chart.
- WHY: the strat made visible at the point of decision; ties chart → Lab coaching.
- STATUS: concept only. *(detail to grow)*

## 8.6 — (open slots — add components as you pour them)
- *e.g. DPMO/volume read, zones, EMAs, VWAP anchor, opening-range — each becomes 8.x with its own status.*

## INDEX LINE
`knowledge/qubed_indicator_spec.md | build, coaching | PUBLIC | living | AREA-8 DEEP SPEC: the Qubed TradingView indicator, component-by-component with build-status (✅done/🛠️borrowed-rewrite/📝spec-needed/⏳not-built). 8.1 consolidate into one/few invite-only indicators (stop the leak). 8.2 invite-only/protected publish. 8.3 easy settings + auto dark/light defaults. 8.4 QUARTERLY LINES = SPEC-NEEDED (explained to past Claudes, never captured — Mark to pour: what/means/why-to-strat). 8.5 live setup grading on chart. Principle: teach the READ not the tool/internals; indicator = wall, coaching loop = moat. GROWS over time.`
